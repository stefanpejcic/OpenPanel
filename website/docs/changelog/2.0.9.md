Not yet released.

### Polish
- MongoDB module is in BETA and available to new installations.
- Removed support for custom plugins (was broken after the 2.0 port from python to go).

### 🐛 Bug Fixes
- [Webmail autologin fails, mailserver stuck in unhealthy state #1131](https://github.com/stefanpejcic/OpenPanel/issues/1131)
- Fixed bug on Site Manager for bulk `update` action on WordPress sites.
- Fixed css bug with `border-b` class missing on header sections in 13 templates.
- Fixed a security issue that allowed FTP account delete/password/path changes to be performed on accounts you don't own.
- Fixed a SQL injection issue in the domain root directory update endpoint.
- Fixed a SQL injection issue in the database size lookup endpoint.
- Fixed a security issue that allowed WP-Manager hardening rules to be changed on domains you don't own.
- Fixed a path traversal issue in the WordPress vulnerability scanner.
- Fixed a security issue that allowed rogue DNS records to be injected via the add DNS record API.
- Fixed a security issue that allowed deleting an email account other than the one specified in the URL.
- Added missing domain name format validation to the domain creation API.
- Hardened several opencli scripts against SQL injection as defense in depth (IP blocking, account backup, permission fixing).
- Fixed a validation bug that could let a custom SSL certificate or key path outside the allowed directory slip through.
