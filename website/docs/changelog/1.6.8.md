Not yet released.

### 💅 Polish
- Added [`terminal_timeout` setting](https://dev.openpanel.com/cli/config.html#terminal_timeout) allowing Administrators to specify max execution time for *OpenPanel > Docker > Terminal* commands.
- Added [`activity_max_size_bytes` setting](https://dev.openpanel.com/cli/config.html#activity-max-size-bytes)allowing Administrators to set file size in bytes to keep for user's Activity Log.
- *OpenPanel > Docker > Terminal* and *OpenAdmin > Advanced > Terminal* will now display info tooltip that terminal is non-interactive and show current `terminal_timeout` value.
- [SSH connection is now validated before adding a slave DNS server in *OpenAdmin > Domains > DNS CLuster*](https://github.com/stefanpejcic/OpenPanel/discussions/756#discussioncomment-14882105).
- 'Websites' menu item added in OpenPanel sidebar.

### 🐛 Bug Fixes
- Fixed bug dispalying html websites count on *OpenPanel > Auto Installer* page.
