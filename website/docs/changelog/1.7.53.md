Not yet released.

### 💅 Polish
- Admin panel is now limited to a single worker if cpu cores are `<=2`.
- Added validations on *OpenPanel > DNS > Edit DNS File* page.
- `alpine` tag will be used for OpenResty webservers of new users, disk size needed is decreased from 511MB to 222MB.
- Improvements to WPManager Updates section.
- [ProxyPass should be used for all domains on account transfer #897](https://github.com/stefanpejcic/OpenPanel/issues/897)
- CPU% and Memory Usage are no longer shown in OpenAdmin UI if user is suspended.
- 'Containers' column is removed from OpenAdmin > Users table.
- Improvements to *OpenPanel > MySQL > Remote Access* page.
- The MariaDB/MySQL database type is now clearly displayed on all database pages.
- Added host validation on all MySQL user pages.
- Improvements to OpenAdmin > Advanced > SSH page.
- Sessions on OpenAdmin are now IP-based - if a user's IP address changes, they will be logged out.
- Added fallback IP check servers for OpenPanel and OpenAdmin.
- Optimized OpenAdmin > Settings > Notifications page when saving.

### 🐛 Bug Fixes
- [Cannot delete Authorized Keys SSH](https://community.openpanel.org/d/282-cannot-delete-authorized-keys-ssh)
- [SSL for hostname wont generate if hostname is set to a subdomain of an existing user domain #915](https://github.com/stefanpejcic/OpenPanel/issues/915)
- [Generate Report on *OpenAdmin > License* does not shows the id message #919](https://github.com/stefanpejcic/OpenPanel/issues/919)
- [Renaming a user with opencli user-rename throws an error #917](https://github.com/stefanpejcic/OpenPanel/issues/917)
- [Bug in OpenAdmin UI - Renaming User results in 500 error #916](https://github.com/stefanpejcic/OpenPanel/issues/916)
- [Incorrect link on admin login page when using separate domains #918](https://github.com/stefanpejcic/OpenPanel/issues/918)
- Fixed `mysql: [ERROR] mysql: option '-e' requires an argument.` on `opencli admin suspend` command.
- Fixed bug on Sentinel that auto-started BIND9 service if zone files exist even whne 'dns' module is disabled.
- Fixed `time data 'for' does not match format '%Y-%m-%d'` on `/json/combined_activity` in OpenAdmin caused by long strings in the long when suer adds a DNS record.
- Fixed bug with screenshots generation in SiteManager when subdirectories are used.
- Fixed bug with Google PageSpeed data checks in SiteManager when subdirectories are used.
- Fixed error on OpenAdmin > Users > Create New when 'send email' option is enabled and no custom port is used for OpenPanel.
- Fixed 404 error on OpenPanel > WP Manager > Security > Harden option after applying the rules, when subdirectory is used.
- Fixed error on OpenPanel > WP Manager > Clone action when subdirectory is set as source domain.
- Fixed JS bug generating random database name on MySQL Databases and Postgres Databases pages.
- Fixed syntax error in Sentinel terminal commands.
