Not yet released.

- Openadmin test: sync_existing_zones_to_slave(slave_ip, master_ip)

### 💅 Polish
- OpenAdmin > Process Manager now uses `SIGKILL` instead of `SIGTERM` to kill pid.

### 🐛 Bug Fixes
