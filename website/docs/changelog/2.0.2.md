Not yet released

### TODO:
- openadmin on /users/<username>#services display PIDs column after Memory - in it show current pids vs the limit from .env - allow edit also as we do for cpu/ram.
- openpanel: on /containers we already have PIDs column, just make it show also the limits form .env file - allow edit also as we do for cpu/ram.
- openpanel: on /website when nodejs/python app we should allow the PIDs to be editable and on install create that env also and use it in the compsoe file.

### Polish
- Added option to set PIDs per container in *OpenAdmin > Settings > User defaults*.
- pgAdmin is removed from OpenPanel UI.
- Docker items are renamed to Containers in OpenPanel UI.
- Added Webmail link in OpenPanel UI sidebar menu and Dashboard icons.

### 🐛 Bug Fixes
- Fixed error adding custom SSL from OpenPanel UI.
- [kill pid on Process Manager not working #1056](https://github.com/stefanpejcic/OpenPanel/issues/1056)
- [Bug in OpenAdmin UI - Podman service showing as disabled while service is active #1055](https://github.com/stefanpejcic/OpenPanel/issues/1055)
- [css bug in OpenAdmin UI on PHP Settings #1043](https://github.com/stefanpejcic/OpenPanel/issues/1043)
- [/emails/reports are no longer working on OpenAdmin in 2.0+ #1048](https://github.com/stefanpejcic/OpenPanel/issues/1048)
