Not yet released.

### 💅 Polish
- The Cron service no longer starts automatically when opening the CronJobs page; it now starts only after a cron job is added, edited, or deleted.
- [Changing PHP Version not showing error message if not enough resources to start the service #794](https://github.com/stefanpejcic/OpenPanel/discussions/794)
- For 'Forceful Server Reboot' on **OpenAdmin > Advanced > Server Reboot** page, the `reboot -f` command is replaced with `echo b > /proc/sysrq-trigger`.
- **Updated [log rotation](https://github.com/stefanpejcic/opencli/blob/main/server/logrotate.sh):** `opencli reports` deleted after 7 days, `opencli update` logs after 3 months, and panel logs rotated except `notifications.log`.
- [Example Python (flask) application](https://github.com/stefanpejcic/python-helloworld)
- [Example NodeJS (express) application](https://github.com/stefanpejcic/nodejs-helloworld)



### 🐛 Bug Fixes
- Fixed an issue in the Redis, Elasticsearch, OpenSearch, and Memcached modules that allowed services to start even when doing so could exceed plan limits.
- Fixed error `Invalid command 'default_page.html', perhaps misspelled or defined by a module not included in the server configuration` for apache when adding a NodeJS/Python Application.
- Fixed error `UnboundLocalError: cannot access local variable '_var_var_27' where it is not associated with a value` for apache when deleting a NodeJS/Python Application.
- Fixed bug checking if .js or .py file exists when adding path on NodeJS/Python Application Installer page.
